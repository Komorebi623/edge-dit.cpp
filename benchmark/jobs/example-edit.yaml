# Image-editing task: base model + distilled (kontext-lightning) across systems.
name: example-edit
task: image-editing
prompts: 3

edge-dit:
  model: [flux-kontext, kontext-lightning]
  quant:
    - {type: fp16, offload: full}
    - q8
diffusers:
  model: [flux-kontext, kontext-lightning]
  quant: [fp8]
