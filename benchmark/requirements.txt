# edge-dit.cpp benchmark dependencies (aggregate / full install).
#
# This file installs the FULL stack (edge-dit + quality metrics + diffusers
# cross-system baseline), preserving the historical behaviour of
# `pip install -r benchmark/requirements.txt`.
#
# Prefer the layered files when you want a leaner install:
#   * only run edge-dit.cpp + score images -> requirements/core.txt
#   * add the diffusers cross-system baseline -> requirements/diffusers.txt
#
# torch is intentionally unpinned: install the build matching your CUDA/driver
# first (see https://pytorch.org), then install this file for the rest.
-r requirements/core.txt
-r requirements/diffusers.txt
