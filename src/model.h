#ifndef LD_MODEL_COMPAT_H
#define LD_MODEL_COMPAT_H

// Compatibility include for model code migrated from stable-diffusion.cpp.
// Those headers include "model.h" for public image types, SDVersion,
// ModelLoader and TensorStorage.
#include "light-dit.h"
#include "core/runtime/model_loader.h"

#endif
